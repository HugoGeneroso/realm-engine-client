learnings/
/.code-health
/.ik-jscpd
/docs

# ── Build artifacts stripped for distribution (regenerate — see SETUP.md) ──
# il2cpp dump (Il2CppInspectorPro output) — ~94 MB, regenerate per game version.
internal/src/game/generated/
# RotMG game data — fetch with `npm run download-game-xml` (see SETUP.md).
client/data/objects.xml
client/data/tiles.xml
internal/data/objects.xml

# ── Extracted Il2CppInspectorRedux tool binaries (regenerate via Expand-Archive from the .zip) ──
Il2CppInspectorRedux/CLI/Il2CppInspectorRedux.CLI-win-x64/
Il2CppInspectorRedux/GUI/
Il2CppInspectorRedux/Legacy-CLI/

# ── Electron build output (regenerate with npm run dist) ──
client/release/

# ── Debug packet captures ──
packets_*.json